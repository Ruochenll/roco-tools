# 2026-07-26

## 视觉升级 · 方向 C「轻游戏化」第一阶段落地

**背景**:用户要对洛克王国工具合集站(Django 5 + HTMX)做视觉/交互优化。选定方向 C:精致浅色为底 + 明暗双主题 + 关键区域游戏氛围。

**已完成的代码改动**:
- `static/css/style.css` 全量重写为设计系统:魔法蓝紫主色 #6D5DF6、琥珀金 #F0B53F、淡紫纸面 #F6F6FA、Noto Serif SC 展示字体 + Noto Sans SC 正文、三档阴影、圆角梯度(8/12/16/999)、`body[data-theme="dark"]` 暗色令牌、fadeUp 入场、骨架屏、HTMX 顶部进度条与请求中内容降透明、属性色 18 种保留原值
- `templates/base.html`:Google Fonts、主题初始化脚本(防 FOUC,localStorage `roco-theme`)、进度条 div、主题切换/汉堡菜单/「/」聚焦搜索/消息自动淡出 JS
- `templates/includes/navbar.html`:星形 logo 标记、链接 pill 激活态(`{% if '/pets/' in request.path %}` 语法,注意 Django 模板不支持 `.startswith` 带参)、主题切换按钮、移动端汉堡按钮(≤900px)
- `templates/core/home.html` 内联样式:hero 改为紫→金渐变衬线标题、光晕氛围、卡片 hairline+16px、快捷入口 hover 紫光、暗色适配
- 旧类名全部保留兼容(pvp/eggs/articles/accounts 页面零改动自动继承新风格)

**验证**:全页面 200;agent-browser(用系统 Edge,`--executable-path` 需先 `agent-browser close` 才生效;`screenshot` 不接 --full-page 参数)截图确认浅色首页、图鉴页、暗色图鉴页、移动端汉堡菜单均正常。

**待办(后续阶段)**:PVP 计算器/属性克制页精细化、伤害数字动画、骨架屏接入具体列表、画布上的图鉴页视觉稿只做完一半(file id 707801481567265,明暗双主题变量已建)。

## 首页按稿重做(第二阶段)

- 用户反馈第一阶段首页只是"换皮",未按画布稿实施。本轮按稿重构 `templates/core/home.html`:Hero 左右双列(动态数据徽章 + 双行衬线大标题 + 双 CTA + 紫渐变立绘面板·白金独角兽·浮动翼/光徽章·热门卡)、四格数据统计条、6 卡工具矩阵、资讯静态三卡(封面缺失按分类渐变兜底,去掉轮播)
- `core/views.py` home 视图新增 pet_count/skill_count/element_count 动态统计(改 .py 需重启服务器)
- `includes/footer.html` 升级为 `.footer-v2` 三段式(品牌+链接组+版权栏),样式在 style.css
- 交付 `MD/design-guide.md` v1.0:令牌/组件/交互/页面模式/工程注意事项,作为全站风格单一事实来源
- 验证:浅色/暗色整页 + 390px 移动端截图均通过,动态数字渲染正确

## 缓存问题排查(用户反馈"样式没生效")

- 用户看到旧样式(工具合集标题消失/卡片无圆角/页脚裸奔):根因是浏览器缓存了旧 style.css——渐变文字依赖新变量 `--color-gold`、圆角依赖 `--radius-lg`,旧 CSS 没有这些定义导致大面积失效
- 修复:① base.html 样式表加版本号 `?v=20260726b`,改 CSS 必 bump(已写入设计指南工程规范);② home.html 内联样式全部加 `var()` 兜底值
- 插曲:还遇到过 runserver 进程服务旧模板的现象(文件已改、响应字节数不变),杀掉 8000 端口全部进程重启后恢复;现在服务器以自动重载模式运行(任务 yw8qF7)
